rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // Note: For production, you should restrict this to authenticated admins
    }
  }
}
