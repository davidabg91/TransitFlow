node_modules
dist
dist-ssr
*.local

# Firebase
.firebase
firebase-debug.log
.firebaserc

# Python bridge build artifacts
__pycache__/
*.pyc
build/

# Secrets — never commit signing keys, service accounts or env files
*.jks
*.p12
*.keystore
*-service-account*.json
.env
.env.*

# Compiled function output — regenerated by the predeploy build
functions/lib/

# Спецификациите на реалните фирми — носят имена и достъпи на живи хора,
# а хранилището е публично. Остават само на машината, която ги пуска.
scripts/companies/*.json
!scripts/companies/example.json
